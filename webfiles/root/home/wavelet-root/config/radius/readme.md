# Note - this directory contains custom parts of the RADIUS server config.
# These are copied during setup to overwrite the files generated from the FreeRADIUS container
# Other than that, this directory is not utilized.